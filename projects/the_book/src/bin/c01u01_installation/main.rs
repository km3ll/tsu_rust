fn main() {
    /*
    pod:
    - rustup
    - linker (C compiler)
    - rustc --version
    - rustup update
    - rustup doc (local documentation)
    - cargo ... --offline (cached dependencies)
     */
    println!("Installation")
}